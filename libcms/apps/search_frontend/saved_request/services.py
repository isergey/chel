# from apps.ssearch import models as search_models
from .entities import SavedRequest


def save_request(saved_request: SavedRequest, user):
    # saved_request_model = saved_request.to_model(user)
    # saved_request_model.save()
    pass